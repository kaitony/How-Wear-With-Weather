/**
 * MainServerBlock.tsx
 * 메인 페이지의 서버 레이아웃 컴포넌트
 *
 * 날씨 정보와 옷차림 추천을 보여주는 메인 화면의 레이아웃을 정의한다.
 * 현재 기본 그라데이션 배경만 적용된 상태.
 */

export default function MainServerBlock() {
  return <div className={`min-h-screen bg-gradient-to-br pb-6`}></div>;
}
